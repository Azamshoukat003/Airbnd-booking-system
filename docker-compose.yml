version: '3.9'
services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    env_file:
      - .env
  booking-widget:
    build: ./frontend/booking-widget
    ports:
      - "5173:5173"
  admin-dashboard:
    build: ./frontend/admin-dashboard
    ports:
      - "5174:5174"
